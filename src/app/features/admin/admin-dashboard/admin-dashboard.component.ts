import {Component,ChangeDetectorRef,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {RouterLink} from '@angular/router';import {finalize,forkJoin} from 'rxjs';import {ApiService} from '../../../core/api.service';
@Component({selector:'app-admin-dashboard',standalone:true,imports:[CommonModule,RouterLink],templateUrl:'./admin-dashboard.component.html',styleUrl:'./common.css'}) export class AdminDashboardComponent{api=inject(ApiService);cdr=inject(ChangeDetectorRef);loading=true;d:any=null;alerts:any=null;ngOnInit(){forkJoin({d:this.api.adminDashboard(),a:this.api.adminAlerts()}).pipe(finalize(()=>{this.loading=false;this.cdr.detectChanges()})).subscribe({next:r=>{this.d=r.d.data.dashboard;this.alerts=r.a.data.alerts},error:()=>{}})} totalAlerts(){const c=this.alerts?.counts||{};return Object.values(c).reduce((a:any,b:any)=>a+Number(b||0),0) as number}}
