import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {ActivatedRoute,RouterLink} from '@angular/router';import {ApiService} from '../../../core/api.service';import {ProduceListing,Cooperative} from '../../../core/models';
import {finalize} from 'rxjs';
@Component({selector:'app-cooperative-profile',standalone:true,imports:[CommonModule,RouterLink],templateUrl:'./cooperative-profile.component.html',styleUrl:'./cooperative-profile.component.css'}) export class CooperativeProfileComponent{cdr=inject(ChangeDetectorRef);api=inject(ApiService);route=inject(ActivatedRoute);coop:Cooperative|null=null;listings:ProduceListing[]=[];loading=true;ngOnInit(){const id=this.route.snapshot.paramMap.get('id')!;this.api.marketplace({limit:100}).pipe(finalize(()=>this.cdr.detectChanges())).subscribe({next:r=>{this.listings=r.data.items.filter(x=>x.cooperative.id===id);this.coop=this.listings[0]?.cooperative||null;this.loading=false},error:()=>this.loading=false})} money(v:any){return Number(v||0).toLocaleString()}}
