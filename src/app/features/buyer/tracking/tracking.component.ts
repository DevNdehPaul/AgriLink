import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {ActivatedRoute,RouterLink} from '@angular/router';import {ApiService} from '../../../core/api.service';import {Order} from '../../../core/models';
import {finalize} from 'rxjs';
@Component({selector:'app-tracking',standalone:true,imports:[CommonModule,RouterLink],templateUrl:'./tracking.component.html',styleUrl:'./tracking.component.css'}) export class TrackingComponent{cdr=inject(ChangeDetectorRef);api=inject(ApiService);route=inject(ActivatedRoute);order:Order|null=null;loading=true;steps=['PAYMENT_SECURED','CONFIRMED','READY_FOR_PICKUP','IN_TRANSIT','DELIVERY_REPORTED','COMPLETED'];ngOnInit(){this.api.order(this.route.snapshot.paramMap.get('id')!).pipe(finalize(()=>this.cdr.detectChanges())).subscribe({next:r=>{this.order=r.data.order;this.loading=false},error:()=>this.loading=false})} done(step:string){if(!this.order)return false;return this.steps.indexOf(this.order.status)>=this.steps.indexOf(step) || this.order.status==='COMPLETED'}}
