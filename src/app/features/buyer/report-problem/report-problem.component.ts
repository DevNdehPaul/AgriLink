import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {ActivatedRoute,Router,RouterLink} from '@angular/router';import {ApiService} from '../../../core/api.service';import {finalize} from 'rxjs';
@Component({selector:'app-report-problem',standalone:true,imports:[CommonModule,FormsModule,RouterLink],templateUrl:'./report-problem.component.html',styleUrl:'./report-problem.component.css'})
export class ReportProblemComponent{private cdr=inject(ChangeDetectorRef);private api=inject(ApiService);private route=inject(ActivatedRoute);private router=inject(Router);id='';category='';description='';busy=false;error='';reasons=['Order did not arrive','Wrong quantity','Damaged produce','Wrong produce','Quality problem','Other'];ngOnInit(){this.id=this.route.snapshot.paramMap.get('id')!}submit(){const text=`${this.category}: ${this.description}`.trim();if(!this.category||this.description.trim().length<10)return;this.busy=true;this.error='';this.api.openDispute(this.id,text).pipe(finalize(()=>this.cdr.detectChanges())).subscribe({next:()=>this.router.navigate(['/buyer/orders',this.id]),error:e=>{this.error=e.error?.message||'Unable to open dispute';this.busy=false}})}}
