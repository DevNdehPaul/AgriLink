import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {ActivatedRoute,RouterLink} from '@angular/router';import {ApiService} from '../../../core/api.service';import {ProduceListing} from '../../../core/models';import {finalize} from 'rxjs';
@Component({selector:'app-marketplace',standalone:true,imports:[CommonModule,FormsModule,RouterLink],templateUrl:'./marketplace.component.html',styleUrl:'./marketplace.component.css'})
export class MarketplaceComponent{private cdr=inject(ChangeDetectorRef);private api=inject(ApiService);private route=inject(ActivatedRoute);listings:ProduceListing[]=[];loading=true;error='';q='';city='';sort='harvest_desc';minPrice?:number;maxPrice?:number;ngOnInit(){this.q=this.route.snapshot.queryParamMap.get('q')||'';this.load()}load(){this.loading=true;this.error='';this.api.marketplace({name:this.q||undefined,city:this.city||undefined,minPrice:this.minPrice,maxPrice:this.maxPrice,sort:this.sort,limit:12}).pipe(finalize(()=>this.cdr.detectChanges())).subscribe({next:r=>{this.listings=r.data.items;this.loading=false},error:e=>{this.error=e.error?.message||'Unable to load marketplace';this.loading=false}})}clear(){this.q='';this.city='';this.minPrice=undefined;this.maxPrice=undefined;this.sort='harvest_desc';this.load()}money(v:any){return Number(v||0).toLocaleString()}}
