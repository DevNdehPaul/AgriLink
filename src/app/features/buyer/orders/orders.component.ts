import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {RouterLink} from '@angular/router';import {ApiService} from '../../../core/api.service';import {Order} from '../../../core/models';import {finalize} from 'rxjs';
@Component({selector:'app-orders',standalone:true,imports:[CommonModule,RouterLink],templateUrl:'./orders.component.html',styleUrl:'./orders.component.css'})
export class OrdersComponent{private cdr=inject(ChangeDetectorRef);private api=inject(ApiService);orders:Order[]=[];loading=true;error='';filter='ALL';ngOnInit(){this.load()}load(){this.loading=true;this.error='';this.api.orders(20).pipe(finalize(()=>this.cdr.detectChanges())).subscribe({next:r=>{this.orders=r.data.items;this.loading=false},error:e=>{this.error=e.error?.message||'Unable to load orders';this.loading=false}})}visible(){if(this.filter==='ALL')return this.orders;if(this.filter==='ACTIVE')return this.orders.filter(o=>!['COMPLETED','CANCELLED','DISPUTED'].includes(o.status));return this.orders.filter(o=>o.status===this.filter)}money(v:any){return Number(v||0).toLocaleString()}}
