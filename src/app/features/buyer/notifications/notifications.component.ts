import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {RouterLink} from '@angular/router';import {forkJoin,finalize} from 'rxjs';import {ApiService} from '../../../core/api.service';
@Component({selector:'app-notifications',standalone:true,imports:[CommonModule,RouterLink],templateUrl:'./notifications.component.html',styleUrl:'./notifications.component.css'}) export class NotificationsComponent{cdr=inject(ChangeDetectorRef);api=inject(ApiService);activity:any[]=[];ngOnInit(){forkJoin({o:this.api.orders(),d:this.api.disputes()}).pipe(finalize(()=>this.cdr.detectChanges())).subscribe({next:r=>{this.activity=[...r.o.data.items.slice(0,8).map(o=>({title:o.orderNumber,text:`Order is ${o.status.replaceAll('_',' ').toLowerCase()}`,date:o.createdAt,link:'/buyer/orders/'+o.id})),...(r.d.data.disputes||[]).slice(0,5).map((d:any)=>({title:'Dispute update',text:d.status,date:d.createdAt,link:'/buyer/orders/'+d.orderId}))].sort((a,b)=>new Date(b.date).getTime()-new Date(a.date).getTime())}})}}
