import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {RouterLink,ActivatedRoute,Router} from '@angular/router';import {finalize} from 'rxjs';import {ApiService} from '../../../core/api.service';import {AuthService} from '../../../core/auth.service';
@Component({selector:'app-seller-listing-detail',standalone:true,imports:[CommonModule,FormsModule,RouterLink],templateUrl:'./seller-listing-detail.component.html',styleUrl:'./seller-listing-detail.component.css'}) export class SellerListingDetailComponent{api=inject(ApiService);route=inject(ActivatedRoute);cdr=inject(ChangeDetectorRef);p:any=null;loading=true;ngOnInit(){this.api.produce(this.route.snapshot.paramMap.get('id')!).pipe(finalize(()=>{this.loading=false;this.cdr.detectChanges()})).subscribe({next:r=>this.p=r.data.listing,error:()=>{}})}sold(){return this.p?Math.max(0,+this.p.totalQuantityKg-+this.p.availableQuantityKg-+(this.p.reservedQuantityKg||0)):0}}
