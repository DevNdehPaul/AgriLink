import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {RouterLink,ActivatedRoute,Router} from '@angular/router';import {finalize} from 'rxjs';import {ApiService} from '../../../core/api.service';import {AuthService} from '../../../core/auth.service';
@Component({selector:'app-seller-notifications',standalone:true,imports:[CommonModule,FormsModule,RouterLink],templateUrl:'./seller-notifications.component.html',styleUrl:'./seller-notifications.component.css'}) export class SellerNotificationsComponent{api=inject(ApiService);cdr=inject(ChangeDetectorRef);items:any[]=[];ngOnInit(){this.api.orders(30).pipe(finalize(()=>this.cdr.detectChanges())).subscribe({next:r=>this.items=r.data.items,error:()=>{}})}message(o:any){if(o.status==='COMPLETED')return 'Delivery confirmed and funds released';if(o.status==='DISPUTED')return 'A dispute is open on this order';if(o.shipment?.status==='ASSIGNED')return 'Driver assigned for pickup';if(o.shipment?.status==='IN_TRANSIT')return 'Produce is in transit';if(o.status==='PAYMENT_SECURED')return 'Buyer payment secured';return 'Order status updated'}}
