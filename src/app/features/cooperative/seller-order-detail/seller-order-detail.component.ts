import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {RouterLink,ActivatedRoute,Router} from '@angular/router';import {finalize} from 'rxjs';import {ApiService} from '../../../core/api.service';import {AuthService} from '../../../core/auth.service';
@Component({selector:'app-seller-order-detail',standalone:true,imports:[CommonModule,FormsModule,RouterLink],templateUrl:'./seller-order-detail.component.html',styleUrl:'./seller-order-detail.component.css'}) export class SellerOrderDetailComponent{api=inject(ApiService);route=inject(ActivatedRoute);cdr=inject(ChangeDetectorRef);o:any=null;loading=true;ngOnInit(){this.api.order(this.route.snapshot.paramMap.get('id')!).pipe(finalize(()=>{this.loading=false;this.cdr.detectChanges()})).subscribe({next:r=>this.o=r.data.order,error:()=>{}})}secured(){return ['PAYMENT_SECURED','CONFIRMED','ASSIGNED','PICKED_UP','IN_TRANSIT','DELIVERY_REPORTED','COMPLETED','DISPUTED'].includes(this.o?.status)}}
