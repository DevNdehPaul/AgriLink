import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {RouterLink,ActivatedRoute,Router} from '@angular/router';import {finalize} from 'rxjs';import {ApiService} from '../../../core/api.service';import {AuthService} from '../../../core/auth.service';
@Component({selector:'app-seller-produce',standalone:true,imports:[CommonModule,FormsModule,RouterLink],templateUrl:'./seller-produce.component.html',styleUrl:'./seller-produce.component.css'}) export class SellerProduceComponent{api=inject(ApiService);auth=inject(AuthService);cdr=inject(ChangeDetectorRef);items:any[]=[];loading=true;ngOnInit(){this.api.marketplace({limit:100}).pipe(finalize(()=>{this.loading=false;this.cdr.detectChanges()})).subscribe({next:r=>{const id=this.auth.profile()?.id;this.items=r.data.items.filter(x=>!id||x.cooperativeId===id)},error:()=>{}})}sold(p:any){return Math.max(0,Number(p.totalQuantityKg||0)-Number(p.availableQuantityKg||0)-Number(p.reservedQuantityKg||0))}}
