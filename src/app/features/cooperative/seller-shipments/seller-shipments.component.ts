import {Component,inject,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {RouterLink,ActivatedRoute,Router} from '@angular/router';import {finalize} from 'rxjs';import {ApiService} from '../../../core/api.service';import {AuthService} from '../../../core/auth.service';
@Component({selector:'app-seller-shipments',standalone:true,imports:[CommonModule,FormsModule,RouterLink],templateUrl:'./seller-shipments.component.html',styleUrl:'./seller-shipments.component.css'}) export class SellerShipmentsComponent{api=inject(ApiService);cdr=inject(ChangeDetectorRef);items:any[]=[];loading=true;ngOnInit(){this.api.orders(50).pipe(finalize(()=>{this.loading=false;this.cdr.detectChanges()})).subscribe({next:r=>this.items=r.data.items.filter(o=>o.shipment),error:()=>{}})}}
