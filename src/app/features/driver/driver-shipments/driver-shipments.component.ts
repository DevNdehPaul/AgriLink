import {Component,ChangeDetectorRef,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {RouterLink} from '@angular/router';import {finalize} from 'rxjs';import {ApiService} from '../../../core/api.service';
@Component({selector:'app-driver-shipments',standalone:true,imports:[CommonModule,RouterLink],templateUrl:'./driver-shipments.component.html',styleUrl:'./driver-shipments.component.css'}) export class DriverShipmentsComponent{api=inject(ApiService);cdr=inject(ChangeDetectorRef);items:any[]=[];loading=true;tab='ACTIVE';ngOnInit(){this.api.driverShipments().pipe(finalize(()=>{this.loading=false;this.cdr.detectChanges()})).subscribe({next:r=>this.items=r.data.shipments,error:()=>{}})}get filtered(){if(this.tab==='ALL')return this.items;if(this.tab==='ACTIVE')return this.items.filter(s=>['ASSIGNED','PICKED_UP','IN_TRANSIT'].includes(s.status));if(this.tab==='DONE')return this.items.filter(s=>['DELIVERY_REPORTED','DELIVERED'].includes(s.status));return this.items}}
