import {Component,inject,signal,ChangeDetectorRef} from '@angular/core';import {CommonModule} from '@angular/common';import {RouterLink,RouterLinkActive,RouterOutlet} from '@angular/router';import {finalize} from 'rxjs';import {AuthService} from '../../core/auth.service';
@Component({selector:'app-cooperative-shell',standalone:true,imports:[CommonModule,RouterLink,RouterLinkActive,RouterOutlet],templateUrl:'./cooperative-shell.component.html',styleUrl:'./cooperative-shell.component.css'}) export class CooperativeShellComponent{auth=inject(AuthService);private cdr=inject(ChangeDetectorRef);menu=signal(false);constructor(){this.auth.me().pipe(finalize(()=>this.cdr.detectChanges())).subscribe({error:()=>{}})} initials(){return (this.auth.profile()?.name||this.auth.user()?.fullName||'CO').split(' ').slice(0,2).map((x:string)=>x[0]).join('').toUpperCase()}}
