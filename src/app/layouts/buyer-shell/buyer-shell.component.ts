import {Component,inject,signal} from '@angular/core';import {Router,RouterLink,RouterLinkActive,RouterOutlet} from '@angular/router';import {FormsModule} from '@angular/forms';import {AuthService} from '../../core/auth.service';
@Component({selector:'app-buyer-shell',standalone:true,imports:[RouterOutlet,RouterLink,RouterLinkActive,FormsModule],templateUrl:'./buyer-shell.component.html',styleUrl:'./buyer-shell.component.css'}) export class BuyerShellComponent{auth=inject(AuthService);router=inject(Router);search='';menu=signal(false);ngOnInit(){} goSearch(){this.router.navigate(['/buyer/marketplace'],{queryParams:this.search.trim()?{q:this.search.trim()}: {}});this.menu.set(false)} initials(){return (this.auth.user()?.fullName||'Buyer').split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase()} }
